# Active toolchains for imgcli.
# The current repository content is the npm-backed documentation site.

javascript:
  packageManager: 'npm'
  inferTasksFromScripts: false

npm: {}

node:
  version: '22.22.2'
  syncVersionManagerConfig: 'nvm'

# Add the Go toolchain when the CLI implementation lands.
